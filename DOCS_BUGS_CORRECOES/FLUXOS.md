# 🔄 Fluxos do Bro App

Este documento descreve os fluxos principais do aplicativo.

---

## 📋 Fluxo de Criação de Ordem (Usuário)

```
┌─────────────────────────────────────────────────────────────┐
│                    MARKETPLACE SCREEN                        │
├─────────────────────────────────────────────────────────────┤
│ 1. Usuário seleciona tipo de conta (PIX, Boleto, etc)       │
│ 2. Usuário insere código da conta                           │
│ 3. Usuário insere valor em BRL                              │
│ 4. Sistema calcula valor em sats (cotação atual)            │
│ 5. Usuário clica "Continuar"                                │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                    PAYMENT SCREEN                            │
├─────────────────────────────────────────────────────────────┤
│ 1. Sistema gera invoice Lightning (via Breez SDK)           │
│ 2. Exibe QR code e string da invoice                        │
│ 3. Aguarda pagamento da invoice                             │
│ 4. ⭐ IMPORTANTE: Ordem NÃO existe ainda!                    │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│               APÓS PAGAMENTO CONFIRMADO                      │
├─────────────────────────────────────────────────────────────┤
│ 1. orderProvider.createOrder() - cria ordem local           │
│ 2. Ordem publicada no Nostr (kind 30078)                    │
│ 3. Status inicial: 'pending' (Aguardando Bro)               │
│ 4. Navega para ORDER STATUS SCREEN                          │
└─────────────────────────────────────────────────────────────┘
```

---

## 👨‍💼 Fluxo do Bro (Provedor)

```
┌─────────────────────────────────────────────────────────────┐
│                PROVIDER ORDERS SCREEN                        │
├─────────────────────────────────────────────────────────────┤
│ 1. orderProvider.fetchOrders(forProvider: true)             │
│ 2. Busca TODAS as ordens pendentes do Nostr                 │
│ 3. Exibe lista de ordens disponíveis para aceitar           │
│ 4. _isProviderMode = true (permite ver ordens de outros)    │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                    BRO ACEITA ORDEM                          │
├─────────────────────────────────────────────────────────────┤
│ 1. Bro clica em uma ordem                                   │
│ 2. Sistema verifica tier (limite de valor)                  │
│ 3. Se OK: publica aceite no Nostr (kind 30079)              │
│ 4. Status: 'pending' → 'accepted'                           │
│ 5. Ordem marcada com providerId = pubkey do Bro             │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                    BRO FAZ PAGAMENTO                         │
├─────────────────────────────────────────────────────────────┤
│ 1. Bro paga a conta (PIX, boleto, etc) no mundo real        │
│ 2. Bro tira foto/screenshot do comprovante                  │
│ 3. Bro envia comprovante via Nostr (kind 30081)             │
│ 4. Status: 'accepted' → 'awaiting_confirmation'             │
│ 5. Comprovante salvo no metadata (proofImage/paymentProof)  │
└─────────────────────────────────────────────────────────────┘
```

---

## ✅ Fluxo de Confirmação (Usuário)

```
┌─────────────────────────────────────────────────────────────┐
│                ORDER STATUS SCREEN                           │
├─────────────────────────────────────────────────────────────┤
│ 1. Usuário vê status 'awaiting_confirmation'                │
│ 2. _buildReceiptCard() exibe comprovante do Bro             │
│ 3. Usuário verifica se pagamento foi feito corretamente     │
│ 4. Usuário clica "Confirmar Pagamento Recebido"             │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                _handleConfirmPayment()                       │
├─────────────────────────────────────────────────────────────┤
│ 1. Pegar providerId da ordem                                │
│ 2. orderProvider.updateOrderStatus(                         │
│      orderId, 'completed', providerId)                      │
│ 3. Publica update no Nostr (kind 30080) com tag #p          │
│ 4. Adiciona ganhos ao saldo do provedor                     │
│ 5. Status: 'awaiting_confirmation' → 'completed'            │
└─────────────────────────────────────────────────────────────┘
```

---

## 💰 Fluxo de Depósito (Tier)

```
┌─────────────────────────────────────────────────────────────┐
│                  DEPOSIT SCREEN - LIGHTNING                  │
├─────────────────────────────────────────────────────────────┤
│ 1. Usuário escolhe tab Lightning                            │
│ 2. Sistema gera invoice (via Breez SDK)                     │
│ 3. Exibe QR code                                            │
│ 4. Polling a cada 5s verificando pagamento                  │
│ 5. Quando pago: atualiza saldo e tier                       │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                  DEPOSIT SCREEN - ON-CHAIN                   │
├─────────────────────────────────────────────────────────────┤
│ 1. Usuário escolhe tab On-chain                             │
│ 2. Sistema gera endereço Bitcoin (via Breez SDK)            │
│ 3. Exibe QR code e endereço                                 │
│ 4. Polling a cada 10s verificando confirmações              │
│ 5. Estados:                                                 │
│    - Aguardando: nenhuma TX detectada                       │
│    - Mempool: TX detectada, 0 confirmações                  │
│    - Confirmando: 1/3, 2/3, 3/3 confirmações                │
│    - Completo: 3 confirmações (proteção RBF)                │
│ 6. Após 3 confirmações: atualiza saldo e tier               │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔄 Fluxo de Sincronização Nostr

### Para Usuário
```dart
syncOrdersFromNostr() {
  1. fetchUserOrders(_currentUserPubkey)  // Ordens criadas pelo usuário
  2. fetchOrderUpdatesForUser()           // Updates (aceites, comprovantes)
  3. Mesclar com ordens locais
  4. _saveOnlyUserOrders()                // Salvar APENAS do usuário
}
```

### Para Provedor (Bro)
```dart
syncAllPendingOrdersFromNostr() {
  1. fetchPendingOrders()                 // TODAS ordens pendentes
  2. fetchUserOrders()                    // Ordens do próprio usuário
  3. fetchOrderUpdatesForProvider()       // Updates para ordens aceitas
  4. Mesclar com ordens locais
  5. _saveOnlyUserOrders()                // Salvar APENAS do usuário
}
```

---

## 📊 Estados de Ordem

```
┌──────────────────┐
│      draft       │  ← Ordem criada mas invoice não paga (removido)
└────────┬─────────┘
         ↓
┌──────────────────┐
│     pending      │  ← Invoice paga, aguardando Bro aceitar
└────────┬─────────┘
         ↓
┌──────────────────┐
│    accepted      │  ← Bro aceitou, vai pagar a conta
└────────┬─────────┘
         ↓
┌──────────────────┐
│awaiting_         │  ← Bro pagou e enviou comprovante
│confirmation      │
└────────┬─────────┘
         ↓
┌──────────────────┐
│    completed     │  ← Usuário confirmou, ordem finalizada ✓
└──────────────────┘

Estados alternativos:
┌──────────────────┐
│    cancelled     │  ← Ordem cancelada
└──────────────────┘

┌──────────────────┐
│    disputed      │  ← Disputa aberta
└──────────────────┘
```

---

## 🔐 Fluxo de Segurança (Login/Logout)

### Login
```dart
1. Usuário insere seed words (12/24 palavras)
2. Deriva chave privada Nostr
3. Salva em SecureStorage
4. orderProvider.loadOrdersForUser(pubkey)
5. Limpa ordens de usuários anteriores
6. Carrega apenas ordens deste usuário
```

### Logout
```dart
1. SecureStorage.clearNostrKeys()
2. SecureStorage.clearProviderMode()
3. orderProvider.clearOrders()
4. Navega para tela de login
```

### Troca de Usuário
```dart
1. Limpar TUDO do usuário anterior
2. _cleanupAnonymousStorage()
3. LocalCollateralService.clearCache()
4. Inicializar com novo pubkey
5. Filtrar ordens por pubkey (segurança)
```

---

*Última atualização: 25 de Janeiro de 2026*
